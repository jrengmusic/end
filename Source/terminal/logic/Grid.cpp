/**
 * @file Grid.cpp
 * @brief Grid is now header-only — all methods are inline in Grid.h.
 *
 * This file is retained as an empty translation unit to avoid build system
 * churn.  Remove it when the CMakeLists / Projucer file list is next updated.
 */

#include "Grid.h"
